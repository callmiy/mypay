(function processNewMetaForm() {
  const formSubmit = document.getElementById("new-meta-form-submit");
  const bodyModalInsertEl = window.appInterface.bodyModalInsertEl;

  if (!formSubmit || bodyModalInsertEl) {
    return;
  }
})();
