
    {{#each shifts as |shift|}}

    <div id="shift-detail-row-{{#if shift.id }}{{shift.id}}{{else}}{{shift._id}}{{/if}}" class="shift-detail-row">
      {{> shiftDetailRowTemplate shift=shift }}
    </div>

    {{/each}}

